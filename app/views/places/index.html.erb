<div class="container">
<h1>Places</h1>

<% if @places.all.count == 0 %>

  <p>There are no places yet, sign up a create the first one!</p>

<% else %>

  <ul>
    <% if @current_user %>  
        <% for place in @places %>
          <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
        <% end %>
      <% else %>
          <% for place in @places %>
          <li><a href="/no_user"><%= place["name"] %></a></li>
        <% end %>
      <% end %>
  </ul>
  
<% end %>

  <p>
    <% if @current_user %>
    <a href="/places/new">New place</a>
    <% end %>
  </p>

</div>